# Inventory-Management-System-Cpp
A hybrid inventory management system that integrates ESP32 hardware authentication with a C++ inventory database. The system retrieves product IDs from ESP32 via serial communication and performs search, add, update, and delete operations on a CSV-based inventory database.
